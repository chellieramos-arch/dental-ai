# DentAI — Cloud Deployment Dependencies
# Streamlit Community Cloud installs from this file automatically.
# Pin major versions to avoid surprises; update intentionally.

# ── Core app ──────────────────────────────────────────────────────────────────
streamlit>=1.50.0
anthropic>=0.97.0
python-dotenv>=1.0.0

# ── LlamaIndex ────────────────────────────────────────────────────────────────
llama-index-core>=0.12.0
llama-index-embeddings-openai>=0.3.0
llama-index-readers-file>=0.4.0
openai>=1.10.0

# ── Vector stores ─────────────────────────────────────────────────────────────
# Cloud (used when MODE=cloud)
# NOTE: chromadb is local-only — install manually with pip for local dev
pinecone>=5.0.0

# ── Supabase (per-student chat history) ───────────────────────────────────────
supabase>=2.0.0

# ── Cookie management (persistent login across page refreshes) ─────────────────
extra-streamlit-components>=0.1.70

# ── PDF rendering ─────────────────────────────────────────────────────────────
PyMuPDF>=1.24.0
